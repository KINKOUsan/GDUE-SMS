## 清理项目冗余文件计划

根据分析，发现以下冗余文件需要清理：

### 前端冗余文件
1. **未使用的组件**（Vite模板默认生成，项目中未使用）：
   - `smsfrontend/src/components/HelloWorld.vue`
   - `smsfrontend/src/components/TheWelcome.vue`
   - `smsfrontend/src/components/WelcomeItem.vue`
   - `smsfrontend/src/components/icons/` 整个目录（5个图标组件）

2. **未使用的样式文件**：
   - `smsfrontend/src/assets/base.css`
   - `smsfrontend/src/assets/main.css`（在main.js中被注释掉）

3. **未使用的资源文件**：
   - `smsfrontend/public/human-resources.png`（代码中未引用）
   - `smsfrontend/public/cover.jpg`（仅在index页面使用，可考虑删除）
   - `smsfrontend/src/assets/logo.svg`（代码中未引用）

### 后端冗余文件
1. **默认生成的文档**：
   - `SMSBackend/HELP.md`（Spring Boot默认帮助文档）

2. **编译产物**（应被.gitignore忽略）：
   - `SMSBackend/target/` 整个目录

3. **Maven Wrapper**：
   - `SMSBackend/.mvn/wrapper/maven-wrapper.jar`

### IDE配置文件
- `smsfrontend/.vscode/settings.json`（个人配置，通常不提交）

### 优化.gitignore
- 确保target目录被正确忽略
- 添加其他可能遗漏的忽略规则

### 执行步骤
1. 删除前端冗余组件和资源
2. 删除后端冗余文件
3. 清理编译产物
4. 优化.gitignore配置